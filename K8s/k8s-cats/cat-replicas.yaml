apiVersion: apps/v1
kind: ReplicaSet
metadata:
  name: cat-replica
  labels:
    app: cats
spec:
  replicas: 3
  selector:
    matchLabels:
      app: cats
  template:
    metadata:
      labels:
        app: cats
    spec:
      containers:
      - name: cat-container
        image: 459956101257.dkr.ecr.us-east-1.amazonaws.com/cat-repo:v0.2
        ports:
        - containerPort: 80
